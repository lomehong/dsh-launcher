<UserControl x:Class="DshLauncher.Gui.Views.PluginsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="ToolbarButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="ToolbarDanger" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush.Error}"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Style="{StaticResource PageContainer}">

            <!-- 页头 -->
            <TextBlock Text="插件管理" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock Text="管理默认与自定义 dsh 插件，缺装可一键补全。"
                       Foreground="{DynamicResource Brush.Muted}" FontSize="13" Margin="0,0,0,20"/>

            <!-- 命令卡片 -->
            <Border Style="{StaticResource Card}" Padding="16,14">
                <WrapPanel>
                    <Button x:Name="BtnRecheck" Style="{StaticResource ToolbarButton}" Content="🔄 重新检测" Click="BtnRecheck_Click"/>
                    <Button x:Name="BtnAddCustom" Style="{StaticResource ToolbarButton}" Content="➕ 添加自定义" Click="BtnAddCustom_Click"/>
                    <Button x:Name="BtnSave" Style="{StaticResource ToolbarButton}" Content="💾 保存更改" Click="BtnSave_Click"/>
                    <Button x:Name="BtnReset" Style="{StaticResource ToolbarDanger}" Content="↩ 重置" Click="BtnReset_Click"/>
                    <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="12,4,0,0"
                               Foreground="{DynamicResource Brush.Muted}" FontSize="12.5" TextWrapping="Wrap"/>
                </WrapPanel>
            </Border>

            <!-- 插件表格卡片 -->
            <Border Style="{StaticResource Card}" Padding="10,8" Margin="0,12,0,0">
                <DataGrid x:Name="PluginGrid"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          AlternatingRowBackground="{DynamicResource Brush.BorderSoft}"
                          RowHeight="38"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          BorderThickness="0"
                          Margin="6,4,6,6">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="启用" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="显示名" Binding="{Binding Display}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="来源" Binding="{Binding SourceKindText}" Width="60" IsReadOnly="True"/>
                        <DataGridTextColumn Header="包名 / 仓库" Binding="{Binding SourceDisplay}" Width="2*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="80" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding StatusBrushKey}" Value="Brush.Ok">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Ok}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding StatusBrushKey}" Value="Brush.Warn">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Warn}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding StatusBrushKey}" Value="Brush.Muted">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Verbose}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="操作" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="删除" Padding="6,2" MinWidth="48" FontSize="11.5"
                                            Click="BtnRemoveRow_Click" Tag="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
