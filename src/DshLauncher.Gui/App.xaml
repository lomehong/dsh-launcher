<Application x:Class="DshLauncher.Gui.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>

            <!-- ========== 暗色 IDE 调色板 ========== -->
            <Color x:Key="C.Primary">#6366F1</Color>
            <Color x:Key="C.PrimaryHover">#818CF8</Color>
            <Color x:Key="C.PrimaryDim">#312E81</Color>
            <Color x:Key="C.Ok">#34D399</Color>
            <Color x:Key="C.OkBg">#052E21</Color>
            <Color x:Key="C.Warn">#FBBF24</Color>
            <Color x:Key="C.WarnBg">#362B08</Color>
            <Color x:Key="C.Error">#F87171</Color>
            <Color x:Key="C.ErrorBg">#3B1214</Color>
            <!-- 背景层次：最深(侧栏) → 面板 → 卡片 -->
            <Color x:Key="C.Bg">#0D1117</Color>
            <Color x:Key="C.Sidebar">#090C10</Color>
            <Color x:Key="C.Panel">#111722</Color>
            <Color x:Key="C.Card">#161D2B</Color>
            <Color x:Key="C.Hover">#1D2739</Color>
            <Color x:Key="C.Border">#232D42</Color>
            <Color x:Key="C.BorderSoft">#1A2234</Color>
            <!-- 文字层次 -->
            <Color x:Key="C.Text">#E6EDF3</Color>
            <Color x:Key="C.TextSoft">#9BA8BD</Color>
            <Color x:Key="C.TextMuted">#5C6A82</Color>

            <SolidColorBrush x:Key="Brush.Primary" Color="{StaticResource C.Primary}"/>
            <SolidColorBrush x:Key="Brush.PrimaryHover" Color="{StaticResource C.PrimaryHover}"/>
            <SolidColorBrush x:Key="Brush.PrimaryDim" Color="{StaticResource C.PrimaryDim}"/>
            <SolidColorBrush x:Key="Brush.Ok" Color="{StaticResource C.Ok}"/>
            <SolidColorBrush x:Key="Brush.OkBg" Color="{StaticResource C.OkBg}"/>
            <SolidColorBrush x:Key="Brush.Warn" Color="{StaticResource C.Warn}"/>
            <SolidColorBrush x:Key="Brush.WarnBg" Color="{StaticResource C.WarnBg}"/>
            <SolidColorBrush x:Key="Brush.Error" Color="{StaticResource C.Error}"/>
            <SolidColorBrush x:Key="Brush.ErrorBg" Color="{StaticResource C.ErrorBg}"/>
            <SolidColorBrush x:Key="Brush.Info" Color="{StaticResource C.Text}"/>
            <SolidColorBrush x:Key="Brush.Verbose" Color="{StaticResource C.TextMuted}"/>
            <SolidColorBrush x:Key="Brush.Muted" Color="{StaticResource C.TextSoft}"/>
            <SolidColorBrush x:Key="Brush.Bg" Color="{StaticResource C.Bg}"/>
            <SolidColorBrush x:Key="Brush.Sidebar" Color="{StaticResource C.Sidebar}"/>
            <SolidColorBrush x:Key="Brush.PanelBg" Color="{StaticResource C.Panel}"/>
            <SolidColorBrush x:Key="Brush.CardBg" Color="{StaticResource C.Card}"/>
            <SolidColorBrush x:Key="Brush.HoverBg" Color="{StaticResource C.Hover}"/>
            <SolidColorBrush x:Key="Brush.Border" Color="{StaticResource C.Border}"/>
            <SolidColorBrush x:Key="Brush.BorderSoft" Color="{StaticResource C.BorderSoft}"/>
            <SolidColorBrush x:Key="Brush.CaptionGlyph" Color="#CFD8E6"/>
            <SolidColorBrush x:Key="Brush.CaptionHover" Color="#16FFFFFF"/>
            <SolidColorBrush x:Key="Brush.CaptionPress" Color="#28FFFFFF"/>
            <SolidColorBrush x:Key="Brush.LogText" Color="#C9D4E4"/>
            <SolidColorBrush x:Key="Brush.LogVerbose" Color="#8B99B0"/>
            <SolidColorBrush x:Key="Brush.LogInfo" Color="#D6DEEA"/>
            <SolidColorBrush x:Key="Brush.PrimaryText" Color="#A5B4FC"/>

            <!-- ========== 全局字体 ========== -->
            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Microsoft YaHei UI, Segoe UI"/>
            </Style>
            <Style TargetType="UserControl">
                <Setter Property="FontFamily" Value="Microsoft YaHei UI, Segoe UI"/>
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
            </Style>

            <!-- ========== 按钮（暗色扁平） ========== -->
            <Style TargetType="Button">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                <Setter Property="Background" Value="{DynamicResource Brush.CardBg}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
                <Setter Property="Padding" Value="14,7"/>
                <Setter Property="MinWidth" Value="84"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" CornerRadius="6"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.HoverBg}"/>
                                    <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource Brush.Verbose}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.Border}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.35"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 主按钮（品牌实心） -->
            <Style x:Key="BtnPrimary" TargetType="Button">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="{DynamicResource Brush.Primary}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" CornerRadius="6"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.PrimaryHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.35"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 危险按钮 -->
            <Style x:Key="BtnDanger" TargetType="Button" BasedOn="{StaticResource BtnPrimary}">
                <Setter Property="Background" Value="{DynamicResource Brush.Error}"/>
            </Style>

            <!-- ========== 侧边导航项 ========== -->
            <Style x:Key="NavItem" TargetType="ListBoxItem">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Muted}"/>
                <Setter Property="FontSize" Value="13.5"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Margin" Value="10,2"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Grid>
                                <!-- 选中指示条 -->
                                <Border x:Name="indicator" Width="3" CornerRadius="2"
                                        HorizontalAlignment="Left" Margin="0,8"
                                        Background="Transparent"/>
                                <Border x:Name="bd" CornerRadius="7" Padding="14,9" Margin="5,0,8,0"
                                        Background="Transparent">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.HoverBg}"/>
                                    <Setter TargetName="indicator" Property="Background" Value="{DynamicResource Brush.Primary}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.BorderSoft}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ========== 卡片 ========== -->
            <Style x:Key="Card" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource Brush.CardBg}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderSoft}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="18,16"/>
                <Setter Property="Margin" Value="0,0,0,12"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
            </Style>
            <Style x:Key="CardTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14.5"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
            </Style>
            <Style x:Key="CardSub" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Muted}"/>
            </Style>

            <!-- 页面级容器：内容居中 + 左右留白 + 限宽（所有 Tab 统一视觉） -->
            <Style x:Key="PageContainer" TargetType="StackPanel">
                <Setter Property="MinWidth" Value="846"/>
                <Setter Property="MaxWidth" Value="846"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="28,24,28,24"/>
            </Style>

            <!-- ========== 输入 ========== -->
            <!-- TextBox：只用默认模板，仅覆盖颜色（自定义模板曾致内容垂直裁剪） -->
            <Style TargetType="TextBox">
                <Setter Property="Padding" Value="9,7"/>
                <Setter Property="Background" Value="{DynamicResource Brush.PanelBg}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                <Setter Property="CaretBrush" Value="{DynamicResource Brush.Primary}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <!-- ComboBox：完整暗色模板（下拉弹层、箭头、输入框全暗） -->
            <Style TargetType="ComboBox">
                <Setter Property="Padding" Value="10,7"/>
                <Setter Property="Height" Value="36"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="toggle" Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="bd" CornerRadius="6"
                                                    Background="{DynamicResource Brush.PanelBg}"
                                                    BorderBrush="{DynamicResource Brush.Border}"
                                                    BorderThickness="1">
                                                <Path Data="M 0 0 L 4 4 L 8 0" Stroke="{DynamicResource Brush.Muted}"
                                                      StrokeThickness="1.5" HorizontalAlignment="Right"
                                                      VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource Brush.Primary}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Left" VerticalAlignment="Center"
                                                  IsHitTestVisible="False"/>
                                <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                       Placement="Bottom" AllowsTransparency="True"
                                       PopupAnimation="Fade" Focusable="False">
                                    <Border CornerRadius="8" Margin="0,4,0,0"
                                            Background="{DynamicResource Brush.CardBg}"
                                            BorderBrush="{DynamicResource Brush.Border}"
                                            BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}">
                                        <ScrollViewer MaxHeight="260" VerticalScrollBarVisibility="Auto">
                                            <StackPanel x:Name="PART_ItemsPresenter"
                                                        IsItemsHost="True" Margin="4"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Muted}"/>
                <Setter Property="Padding" Value="10,7"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="bd" CornerRadius="5" Padding="{TemplateBinding Padding}"
                                    Background="Transparent" Margin="1">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.HoverBg}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Brush.PrimaryDim}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- CheckBox：暗色方块 + 品牌勾选 -->
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal" Background="Transparent">
                                <Border x:Name="box" Width="17" Height="17" CornerRadius="4"
                                        Background="{DynamicResource Brush.PanelBg}"
                                        BorderBrush="{DynamicResource Brush.Border}"
                                        BorderThickness="1.2"
                                        VerticalAlignment="Center">
                                    <Path x:Name="check" Data="M 2.5 6 L 5.5 9 L 10.5 2.5"
                                          Stroke="White" StrokeThickness="2"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="box" Property="Background" Value="{DynamicResource Brush.Primary}"/>
                                    <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource Brush.Primary}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource Brush.Primary}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- RadioButton：暗色圆点 + 品牌选中 -->
            <Style TargetType="RadioButton">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <StackPanel Orientation="Horizontal" Background="Transparent">
                                <Border x:Name="outer" Width="17" Height="17" CornerRadius="9"
                                        Background="{DynamicResource Brush.PanelBg}"
                                        BorderBrush="{DynamicResource Brush.Border}"
                                        BorderThickness="1.2"
                                        VerticalAlignment="Center">
                                    <Ellipse x:Name="dot" Width="7" Height="7"
                                             Fill="{DynamicResource Brush.Primary}"
                                             HorizontalAlignment="Center" VerticalAlignment="Center"
                                             Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="dot" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="outer" Property="BorderBrush" Value="{DynamicResource Brush.Primary}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="outer" Property="BorderBrush" Value="{DynamicResource Brush.Primary}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ========== 进度条 ========== -->
            <Style TargetType="ProgressBar">
                <Setter Property="Height" Value="6"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Primary}"/>
                <Setter Property="Background" Value="{DynamicResource Brush.BorderSoft}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ProgressBar">
                            <Grid ClipToBounds="True">
                                <Border CornerRadius="3" Background="{TemplateBinding Background}"/>
                                <Border x:Name="PART_Track"/>
                                <Border x:Name="PART_Indicator" CornerRadius="3"
                                        Background="{TemplateBinding Foreground}"
                                        HorizontalAlignment="Left"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ========== 滚动条 ========== -->
            <Style TargetType="ScrollBar">
                <Setter Property="Width" Value="9"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border CornerRadius="4" Margin="2"
                                                        Background="{DynamicResource Brush.Border}"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ========== DataGrid ========== -->
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="RowBackground" Value="Transparent"/>
                <Setter Property="AlternatingRowBackground" Value="{DynamicResource Brush.BorderSoft}"/>
                <Setter Property="GridLinesVisibility" Value="None"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource Brush.PanelBg}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Muted}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Padding" Value="10,9"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
            </Style>
            <Style TargetType="DataGridCell">
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="6"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- DataGridRow：默认模板已消费 Background 属性，只设触发器即可（自定义模板曾致布局递归栈溢出） -->
            <Style TargetType="DataGridRow">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Info}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Brush.HoverBg}"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource Brush.PrimaryDim}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
